import React, { useEffect, useState } from 'react'; import axios from './api'; const App = () => { const [tasks, setTasks] = useState([]); const fetchTasks = async () => { const response = await axios.get('/tasks'); setTasks(response.data); }; useEffect(() => { fetchTasks(); }, []); return (<div><h1>Task Manager</h1><ul>{tasks.map(task => <li key={task._id}>{task.title}</li>)}</ul></div>); }; export default App;